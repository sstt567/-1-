/*
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
using namespace std;

int main() {
    setlocale(LC_ALL, "Ru");

    vector<int> vec(10);
    iota(vec.begin(), vec.end(), 1);

    cout << "Вектор: ";
    for_each(vec.begin(), vec.end(), [](int x) { cout << x << " "; });
    cout << endl;

    return 0;
}
*/
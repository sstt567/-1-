/*
#include <iostream>
#include <cmath>
using namespace std;

int main() {
    setlocale(LC_ALL, "RU");
    double r[3][2];
    cout << "Введите координаты вершин треyгольника:\n";
    for (int i = 0; i < 3; i++) {
        cout << "Вершина " << i + 1 << " (x y):";
        cin >> r[i][0] >> r[i][1];
    }
    double C1 = (r[0][0] + r[1][0] + r[2][0]) / 3;
    double C2 = (r[0][1] + r[1][1] + r[2][1]) / 3;

    cout << "Координаты центра масс Cx, Cy:" << C1 <<"," << C2;

    return 0;
}
*/

